#include <stdlib.h>
#include <stdbool.h>
#include "common.h"

/// @brief Shows the stock for the database.
/// @param database, the database operated upon.
void show_stock(ioopm_db_t *database);

/// @brief Lists the existing merch in the database.
/// @param database, the database operated upon.
void list_merch_UI(ioopm_db_t *database);
